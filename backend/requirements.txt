# MedNuskha backend — stack locked in AGENTS.md §6.
# Floors, not pins: this repo runs on Python 3.14 (see PROJECT_LOG.md decisions
# log, Session 1) and several of these only publish 3.14 wheels on newer
# releases. Do not add anything here that is not in AGENTS.md §6.

# --- web ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# --- data ---
sqlmodel>=0.0.22
psycopg[binary]>=3.2.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.1

# --- http (only whatsapp/client.py may use this to reach graph.facebook.com) ---
httpx>=0.27.0

# --- scheduling ---
apscheduler>=3.10.4

# --- alibaba cloud model studio (Qwen + Qwen-VL) ---
dashscope>=1.20.0

# --- voice ---
faster-whisper>=1.0.3
# Microsoft neural TTS with no account, card or key. Replaced
# google-cloud-texttospeech on 2026-08-22 - see PROJECT_LOG.md.
edge-tts>=6.1.9
# PyAV converts the MP3 edge-tts returns into the OGG/Opus mono WhatsApp needs
# (invariant 7). Already pulled in by faster-whisper; pinned here so the
# dependency is explicit rather than accidental. No ffmpeg binary required.
av>=12.0.0

# --- storage ---
supabase>=2.9.0

# --- reports ---
# fpdf2 replaced WeasyPrint on 2026-08-23. WeasyPrint needs GTK/Pango, which
# pip does not provide on Windows, so it could not even import on the dev
# machine. fpdf2 is pure Python and renders identically on Windows and the
# ECS box with no system packages. uharfbuzz is what makes Urdu legible -
# without it Arabic-script letters do not join. See PROJECT_LOG.md.
fpdf2>=2.8.8
uharfbuzz>=0.39.0

# --- tests ---
pytest>=8.3.0
pytest-asyncio>=0.24.0
